# make prompt don't show all path
typeset -g POWERLEVEL9K_SHORTEN_STRATEGY=default
typeset -g POWERLEVEL9K_SHORTEN_DIR_LENGTH=3
typeset -g POWERLEVEL9K_SHORTEN_DELIMITER='..'

# Default current directory color.
typeset -g POWERLEVEL9K_DIR_FOREGROUND=117

# prompt symbol if the last command succeeded.
# typeset -g POWERLEVEL9K_PROMPT_CHAR_OK_{VIINS,VICMD,VIVIS,VIOWR}_FOREGROUND=189


